{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm ci && npm run build",
    "watchPatterns": ["client/**"]
  },
  "deploy": {
    "startCommand": "serve dist -s -l tcp://0.0.0.0:$PORT"
  }
}
